{
  "name": "@blockout/server",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "The Blockout server: serves the built games, runs classrooms (whole class, pairs, tournaments) and accounts (sqlite). Node built-ins only.",
  "dependencies": {
    "@blockout/engine": "*",
    "@blockout/progress": "*"
  },
  "scripts": {
    "dev": "node --watch src/index.js 8080",
    "start": "node src/index.js 8080",
    "test": "node --test",
    "lint": "eslint src test"
  }
}
